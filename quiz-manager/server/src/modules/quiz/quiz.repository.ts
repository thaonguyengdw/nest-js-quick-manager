// import { Repository, DataSource } from 'typeorm';
// import { Quiz } from './quiz.entity';

// /**
//  * The QuizRepository class extends the Repository class from the TypeORM library.
//  * It is used to interact with the Quiz entity in the database
//  */
// export class QuizRepository extends Repository<Quiz> {
//   constructor(private dataSource: DataSource) {
//     super(Quiz, dataSource.createEntityManager());
//   }
// }

// import { Repository, DataSource } from 'typeorm';
// import { Quiz } from './quiz.entity';

// export class QuizRepository extends Repository<Quiz> {
//   constructor(private dataSource: DataSource) {
//     super(Quiz, dataSource.createEntityManager());
//   }

//   // Add any custom repository methods here
// }
